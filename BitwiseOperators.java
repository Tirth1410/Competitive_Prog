import java.sql.SQLOutput;

public class BitwiseOperators {
    public static void main(String args[]) {
        // XOR operator
        // gives zero if both operands are same and 1 if both operands are different
        System.out.println(6 ^ 0);
    }
}